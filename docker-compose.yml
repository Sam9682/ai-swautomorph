services:
  ai-swautomorph:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: ai-swautomorph-app
    ports:
      - "${HTTP_PORT:-6000}:80"
    environment:
      - FLASK_ENV=production
      - SECRET_KEY=${SECRET_KEY:-change-this-in-production}
    volumes:
      - app_data:/app/data
      - ~/.ssh:/home/ubuntu/.ssh:ro
      - /var/run/docker.sock:/var/run/docker.sock
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:${HTTP_PORT:-6000}/api/auth/status"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

  nginx:
    image: nginx:alpine
    container_name: ai-swautomorph-nginx
    ports:
      - "${HTTPS_PORT:-6001}:443"
    volumes:
      - ./conf/nginx.conf:/etc/nginx/nginx.conf:ro
      - ./ssl:/etc/nginx/ssl:ro
    depends_on:
      - ai-swautomorph
    restart: unless-stopped
    environment:
      - HTTP_PORT=${HTTP_PORT:-6000}
      - HTTPS_PORT=${HTTPS_PORT:-6001}

  gitea:
    image: gitea/gitea:1.21.3
    container_name: ai-swautomorph-gitea
    ports:
      - "3000:3000"
    volumes:
      - gitea_data:/data
      - gitea_repos:/home/ubuntu/admin/data/gitea-repositories
      - /etc/timezone:/etc/timezone:ro
      - /etc/localtime:/etc/localtime:ro
    environment:
      - USER_UID=1000
      - USER_GID=1000
      - GITEA__database__DB_TYPE=sqlite3
      - GITEA__database__PATH=/data/gitea/gitea.db
      - GITEA__repository__ROOT=/home/ubuntu/admin/data/gitea-repositories
      - GITEA__server__DOMAIN=localhost
      - GITEA__server__HTTP_PORT=3000
      - GITEA__server__ROOT_URL=http://localhost:3000/
      - GITEA__mailer__ENABLED=false
      - GITEA__service__DISABLE_REGISTRATION=false
      - GITEA__service__REQUIRE_SIGNIN_VIEW=false
      - GITEA__log__MODE=file
      - GITEA__log__LEVEL=Info
      - GITEA__log__ROOT_PATH=/data/gitea/log
      - GITEA__security__INSTALL_LOCK=true
      - GITEA__security__DISABLE_QUERY_AUTH_TOKEN=true
      - GITEA__git.lfs__START_SERVER=true
      - GITEA__git.lfs__CONTENT_PATH=/home/ubuntu/admin
      - GITEA__REPOSITORY__ENABLE_PUSH_CREATE_USER=true
      - GITEA__WEBHOOK__SKIP_TLS_VERIFY=true
      - GITEA__MAILER__SKIP_VERIFY=true
      - INSTALL_LOCK=true
      - GITEA_ADMIN_USERNAME=admin
      - GITEA_ADMIN_PASSWORD=password
      - GITEA_ADMIN_EMAIL=admin@swautomorph.com
    restart: unless-stopped

volumes:
  app_data:
  gitea_data:
  gitea_repos: